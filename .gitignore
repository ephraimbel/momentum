# Xcode / build
.build/
DerivedData/
build/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout

# XcodeGen generates the project from project.yml — regenerate with `xcodegen generate`.
Momentum.xcodeproj/

# Python bytecode from the scripts/ helpers
__pycache__/
*.pyc

# SwiftPM
.swiftpm/
Package.resolved

# macOS
.DS_Store

# Secrets (Supabase / RevenueCat / Superwall keys land here in later phases)
*.xcconfig.local
Secrets.plist
.env
Secrets.xcconfig

# Obsidian vault + personal notes living in the repo dir
.obsidian/
*.canvas
*.base
20[0-9][0-9]-[0-9][0-9]-[0-9][0-9].md
.vercel
